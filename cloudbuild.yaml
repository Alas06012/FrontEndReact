steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/frontend-service:$COMMIT_SHA', '.']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/frontend-service:$COMMIT_SHA']
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - 'gcloud'
      - 'run'
      - 'deploy'
      - 'frontend-service'
      - '--image=gcr.io/$PROJECT_ID/frontend-service:$COMMIT_SHA'
      - '--platform=managed'
      - '--region=us-east1'
      - '--allow-unauthenticated'
      - '--port=5000'
      - '--min-instances=0'
      - '--max-instances=10'
      - '--ingress=all'
      - '--vpc-connector=serverless-connector'
      - '--vpc-egress=all-traffic'
      - '--cpu=1'
      - '--memory=1Gi'
      - '--set-env-vars=VITE_API_HOST=${_VITE_API_HOST},VITE_API_LB_TYPE=${_VITE_API_LB_TYPE}, VITE_API_PORT=${_VITE_API_PORT}'

options:
  logging: CLOUD_LOGGING_ONLY
images:
  - 'gcr.io/$PROJECT_ID/frontend-service:$COMMIT_SHA' 